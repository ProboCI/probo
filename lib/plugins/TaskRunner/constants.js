'use strict';

const MAX_DRUPAL_VERSION = 8;
const MIN_DRUPAL_VERSION = 6;
const DEFAULT_DRUPAL_VERSION = 7;
const DATABASE_PASSWORD = 'strongpassword';
const DATABASE_USER = 'root';
const DRUPAL_DATABASE_NAME = 'drupal';
const WORDPRESS_DATABASE_NAME = 'wordpress';
const MYSQLCNF_PATH='/etc/mysql/mysql.conf.d/';

module.exports = {
  MAX_DRUPAL_VERSION: MAX_DRUPAL_VERSION,
  MIN_DRUPAL_VERSION: MIN_DRUPAL_VERSION,
  DEFAULT_DRUPAL_VERSION: DEFAULT_DRUPAL_VERSION,
  DATABASE_USER: DATABASE_USER,
  DATABASE_PASSWORD: DATABASE_PASSWORD,
  DRUPAL_DATABASE_NAME: DRUPAL_DATABASE_NAME,
  WORDPRESS_DATABASE_NAME: WORDPRESS_DATABASE_NAME,
  MYSQLCNF_PATH: MYSQLCNF_PATH,
};
